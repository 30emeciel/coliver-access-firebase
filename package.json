{
  "name": "coliver-access-firestore",
  "version": "1.0.0",
  "scripts": {
    "test-firestore": "mocha --exit test/firestore.spec.js",
    "test": "mocha --exit test/*.spec.js"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^1.3.1",
    "firebase-admin": "^9.8.0",
    "firebase-tools": "^9.11.0",
    "mocha": "^8.0.1"
  }
}
